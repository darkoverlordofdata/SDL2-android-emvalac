{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            // "cwd": "${workspaceRoot}/install/src",
            // "program": "${workspaceRoot}/install/src/${workspaceRootFolderName}",
            "cwd": "${workspaceRoot}/build",
            "program": "${workspaceRoot}/build/${workspaceRootFolderName}",
            "args": []
        }
    ]
}